export interface Medicine {
  id: string;
  name: string;
  quantity: string;
  unit: string;
  expiration: string;
  inDate: string;
  outDate: string;
  categoryId: string;
  description: string;
}
